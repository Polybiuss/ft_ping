#include "../libft/libft.h"
#include "ft_ping.h"

int main(int argc, char *argv[]) {
    (void)argv;
    if (argc == 1){
        printf("%s: %s\n", NAME_PROG, MISS_H_OPE);
    }
    // for (int i = 0; i < argc; i++)
    // {
    //     printf("%d: %s\n", i, argv[i]);
    // }
    else {
        
    }
    return (0);
}